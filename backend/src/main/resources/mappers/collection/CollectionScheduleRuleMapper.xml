<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bienvenueblainville.collection.CollectionScheduleRuleMapper">
    <select id="findActive" resultType="com.bienvenueblainville.collection.CollectionScheduleRule">
        select id, sector, collection_type, bin_color, anchor_date, interval_weeks,
               generated_through, note_fr, note_en, note_zh, source_url, active
        from collection_schedule_rule
        where active = true
        order by id asc
    </select>

    <update id="advanceGeneratedThrough">
        update collection_schedule_rule
        set generated_through = #{generatedThrough}
        where id = #{id}
          and generated_through &lt; #{generatedThrough}
    </update>
</mapper>
