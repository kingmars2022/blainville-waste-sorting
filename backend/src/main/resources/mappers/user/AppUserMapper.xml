<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bienvenueblainville.user.AppUserMapper">
    <select id="findByEmail" resultType="com.bienvenueblainville.user.AppUser">
        select id, email, password_hash, role, created_at, updated_at
        from app_user
        where email = #{email}
    </select>

    <select id="findById" resultType="com.bienvenueblainville.user.AppUser">
        select id, email, password_hash, role, created_at, updated_at
        from app_user
        where id = #{id}
    </select>

    <select id="countByRole" resultType="long">
        select count(*)
        from app_user
        where role = #{role}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into app_user (email, password_hash, role)
        values (#{email}, #{passwordHash}, #{role})
    </insert>
</mapper>
