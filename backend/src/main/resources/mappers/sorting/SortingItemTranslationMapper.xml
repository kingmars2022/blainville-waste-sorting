<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bienvenueblainville.sorting.SortingItemTranslationMapper">
    <select id="findByItemId" resultType="com.bienvenueblainville.sorting.SortingItemTranslation">
        select id, item_id, language_code, name, instruction, location, availability
        from sorting_item_translation
        where item_id = #{itemId}
    </select>

    <select id="findByItemIds" resultType="com.bienvenueblainville.sorting.SortingItemTranslation">
        select id, item_id, language_code, name, instruction, location, availability
        from sorting_item_translation
        where item_id in
        <foreach collection="itemIds" item="itemId" open="(" separator="," close=")">#{itemId}</foreach>
    </select>

    <insert id="insert">
        insert into sorting_item_translation
            (item_id, language_code, name, instruction, location, availability)
        values (#{itemId}, #{languageCode}, #{name}, #{instruction}, #{location}, #{availability})
    </insert>

    <!-- Every translation, for the public guide: one query instead of one per
         item, which at 15 items and 3 languages is 45 round trips saved. -->
    <select id="findAll" resultType="com.bienvenueblainville.sorting.SortingItemTranslation">
        select id, item_id, language_code, name, instruction, location, availability
        from sorting_item_translation
    </select>

    <delete id="deleteByItemId">
        delete from sorting_item_translation where item_id = #{itemId}
    </delete>
</mapper>
