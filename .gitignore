# dependencies / build output
node_modules/
.next/
dist/
build/
out/
target/
*.class
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# secrets  —  NEVER commit these
.env
.env.*
!.env.example
*.pem
*.key

# local tooling
.DS_Store
.idea/
.vscode/
.m2/
.npm-cache/
*.log
*.tsbuildinfo

# The built frontend, copied into the backend by the Docker build so one image
# serves both. Generated, never edited, and 150 KB of minified noise in a diff.
backend/src/main/resources/static/
